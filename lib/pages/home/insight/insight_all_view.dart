import 'package:flutter/material.dart';

class InsightAllView extends StatelessWidget {
  const InsightAllView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Insight All View（仮）'),
    );
  }
}
