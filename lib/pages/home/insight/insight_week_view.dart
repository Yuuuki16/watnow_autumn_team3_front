import 'package:flutter/material.dart';

class InsightWeekView extends StatelessWidget {
  const InsightWeekView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Insight Week View（仮）'),
    );
  }
}
